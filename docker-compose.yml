services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - APIMART_API_KEY=${APIMART_API_KEY}
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
